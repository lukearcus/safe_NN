Next steps: start work on barrier certificates which should offer an easier problem

Structutal one would be better with a quadratic output layer, but need to implement this manually...

Lyapunov struggles when init mean is 0, some trajectories presumably getting very close to origin
